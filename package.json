{
  "name": "polynomial",
  "version": "0.0.1",
  "description": "root finding of any degree polynomials",
  "main": "index.js",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register ./src/**/*.test.js --recursive",
    "build": "babel src --presets babel-preset-es2015 --out-file index.js --ignore \"**/*.test.js\"",
    "prepublish": "npm run build",
    "test:single": "mocha --compilers js:babel-core/register"
  },
  "keywords": [
    "math",
    "polynomials",
    "fp"
  ],
  "author": "Renan Vaz",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.22.0",
    "babel-preset-es2015": "^6.22.0",
    "babel-core": "^6.22.0",
    "mocha": "^3.1.2",
    "chai": "^3.5.0"
  }
}
